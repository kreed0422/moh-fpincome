import { CollectionNoticeDirective } from './collection-notice.directive';

describe('CollectionNoticeDirective', () => {
  it('should create an instance', () => {
    const directive = new CollectionNoticeDirective();
    expect(directive).toBeTruthy();
  });
});
