import { Directive } from '@angular/core';

/** TODO: Use FPCare Public Web as template for directive to keep focus within modal */
@Directive({
  selector: '[fpirCollectionNotice]',
})
export class CollectionNoticeDirective {
  constructor() {}
}
